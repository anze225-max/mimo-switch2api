# MiMo Switch

**MiMo Switch** —— 把 MiMo 桌面端的免费额度暴露成一个本地 **OpenAI 协议**端点的单文件 Windows 工具。

- **本地 OpenAI 协议端点** —— 包装成 `http://127.0.0.1:7864/v1`，任何支持 OpenAI 接口的工具（填 `OPENAI_BASE_URL` / `OPENAI_API_KEY` / `model`）都能直接用，支持图片输入
- **工具内登录** —— 窗口里显示小米官方登录页，登录一次即可；凭据只存本机（DPAPI 加密），不经过任何第三方
- **面板** —— 浏览器打开 `http://127.0.0.1:7864/` 查看状态、剩余额度、用量与接入配置
- **单 exe 自安装** —— 双击运行选一次安装目录即可：开机静默自启、崩溃自动重启、保活续期均可配置
- **一键干净卸载** —— 安装目录里自带「卸载 MiMo Switch.exe」，卸载不留残留

> MiMo 不必安装或保持运行；额度、用量与倍率均来自上游返回的真实数据。

## 下载

目前仅支持 Windows 系统。从 [GitHub Releases](../../releases/latest) 下载 `MiMoSwitch.exe`（就这一个文件）双击运行：

1. 首次运行弹出目录选择框，选定安装位置（需当前用户可写，例如 `D:\MiMoSwitch`；系统目录会被拒绝）
2. 安装完成后自动注册开机自启（可在面板关闭），托盘常驻，浏览器打开 `http://127.0.0.1:7864/`
3. 面板「接入配置」给出三项：接口地址、API Key（本地令牌）、模型 —— 本端点使用 **OpenAI 协议**，填进任何支持 OpenAI 接口的工具即可

```env
OPENAI_BASE_URL=http://127.0.0.1:7864/v1
OPENAI_API_KEY=<面板里的本地令牌>
model=mimo-v2.6-flash
```

没有小米账号登录态时，点面板上的「登录／重新授权小米账号」，在弹出窗口里走小米官方登录页。如果本机装有 MiMo 桌面端且已登录，也可以直接采纳其登录态：

```bash
mimo-switch.exe harvest --launch
```

### 卸载

三个入口都一样：安装目录里的「卸载 MiMo Switch.exe」、开始菜单快捷方式、或命令行 `mimo-switch.exe uninstall`。确认框会列出将要删除的内容（开机自启项、快捷方式、安装目录、配置与凭证），点「是」即全部清掉，安装文件夹也会删干净、不留残留。想保留账号凭证（下次装回来不用重新登录）加 `--keep-config`。

## 功能

| 模块 | 说明 |
| --- | --- |
| OpenAI 协议端点 | `POST /v1/chat/completions`（流式与非流式、图片输入）、`GET /v1/models`、`GET /` 面板 |
| 工具内登录 | 弹窗显示小米官方登录页，OAuth 由用户本人完成；支持自动续期与保活 |
| 会话采纳 | `harvest --launch` 从本机 MiMo 桌面端采纳已登录会话，无需重新登录 |
| 面板 | 状态、额度来源、剩余额度、真实 token 用量统计、可用模型与倍率 |
| 启动与保活 | 开机静默自启、端点中断自动重启、保活续期间隔，均可在面板配置 |
| 自安装 | 首次运行选择安装目录（记录后不再询问）；安装目录内自带卸载副本 |
| 一键卸载 | 删自启项、快捷方式、安装目录、配置与凭证；目录里有陌生文件时会保留并提示 |

## 自行构建

需要 Go 1.26+（Windows x64，`CGO_ENABLED=0`）：

```bash
go build -trimpath -buildvcs=false -ldflags "-H=windowsgui -s -w" -o bin/MiMoSwitch.exe ./cmd/switch
bash tools/make-release.sh    # 产出 dist/MiMoSwitch.exe
```

## 致谢

- **[cv-superding/WorkBuddy-Switch2api](https://github.com/cv-superding/WorkBuddy-Switch2api)** —— 感谢该开源项目。

## 免责声明

1. 本项目是**非官方第三方个人工具**，仅供学习与研究用途。
2. 登录通过小米官方页面由用户本人完成，账号凭证仅加密存储在用户本机，本项目不收集、不上传任何账号数据。
3. 使用者须自行确保使用行为符合小米的服务条款、开发者协议及所在地法律法规。**因使用本项目（包括但不限于账号封禁、额度调整、服务变更）产生的一切后果由使用者自行承担**。
4. 本软件按「现状」（AS IS）提供，不提供任何明示或默示担保；在适用法律允许的最大范围内，作者不对任何直接或间接损失负责。

若小米或其权利人认为本项目任何内容不当，可提交 Issue 或直接联系仓库作者，将在第一时间处理（包括下架）。

## 许可

[MIT](./LICENSE)
